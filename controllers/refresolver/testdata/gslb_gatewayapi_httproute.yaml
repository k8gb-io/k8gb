apiVersion: k8gb.io/v1beta1
kind: Gslb
metadata:
  name: gatewayapi-httproute
  namespace: test-gslb
spec:
  resourceRef:
    apiVersion: gateway.networking.k8s.io/v1
    kind: HTTPRoute
    name: gatewayapi-httproute
  strategy:
    type: roundRobin
    dnsTtlSeconds: 30

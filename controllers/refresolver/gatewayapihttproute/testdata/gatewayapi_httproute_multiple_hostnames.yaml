apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gatewayapi-httproute
  namespace: test-gslb
spec:
  parentRefs:
  - name: gatewayapi-gateway
  hostnames:
  - gatewayapi-httproute1.cloud.example.com
  - gatewayapi-httproute2.cloud.example.com
  rules:
  - backendRefs:
    - name: gatewayapi-httproute-service
      port: 8080

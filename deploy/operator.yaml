apiVersion: apps/v1
kind: Deployment
metadata:
  name: ohmyglb
  namespace: ohmyglb
spec:
  replicas: 1
  selector:
    matchLabels:
      name: ohmyglb
  template:
    metadata:
      labels:
        name: ohmyglb
    spec:
      serviceAccountName: ohmyglb
      containers:
        - name: ohmyglb
          # Replace this with the built image name
          image: ytsarev/ohmyglb:v0.2.0
          command:
          - ohmyglb
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              value: ""
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "ohmyglb"

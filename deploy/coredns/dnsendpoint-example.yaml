apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: examplednsrecord
spec:
  endpoints:
  - dnsName: foo.absa.external
    recordTTL: 180
    recordType: A
    targets:
    - 10.1.1.1
